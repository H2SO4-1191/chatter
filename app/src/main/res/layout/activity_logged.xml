<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.LoggedActivity"
    android:background="@color/black">
    <ProgressBar
        android:id="@+id/loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:indeterminate="true"
        android:indeterminateTint="@color/seriousYellow"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.5" />
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationDrawer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@color/onBlack"
        app:headerLayout="@layout/header"
        app:menu="@menu/main_menu"
        app:subheaderColor="@color/seriousYellow"
        app:itemTextColor="@color/white"
        app:itemIconTint="@color/seriousYellow"
        app:itemRippleColor="@color/seriousYellow"
        app:drawerLayoutCornerSize="0dp"
        android:layout_gravity="start"/>
    <include android:id="@+id/chatChattersLayout"
        layout="@layout/chat_chatters_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <include android:id="@+id/verificationLayoutInclude"
        layout="@layout/verification_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"/>

</androidx.drawerlayout.widget.DrawerLayout>