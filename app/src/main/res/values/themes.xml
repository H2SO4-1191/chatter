<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Chatter" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/onBlack</item>
        <item name="colorPrimaryDark">@color/onBlack</item>
        <item name="colorAccent">@color/onBlack</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="statusBarBackground">@color/onBlack</item>
    </style>
    <style name="Theme.Chatter" parent="Base.Theme.Chatter" />
</resources>